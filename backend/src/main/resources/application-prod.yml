spring:
  jpa:
    show-sql: false

server:
  servlet:
    session:
      cookie:
        secure: true

logging:
  level:
    pl.piomin.services: INFO
    org.springframework.security: INFO
    org.hibernate.SQL: WARN
